/* modal.css - полная версия с уменьшенными мобильными размерами */

.modal-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.95);
    backdrop-filter: blur(12px);
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    animation: fadeIn 0.3s ease;
    isolation: isolate;
}

@keyframes fadeIn {
    from {
        opacity: 0;
    }
    to {
        opacity: 1;
    }
}

.modal-content {
    background: linear-gradient(135deg, #0a0808, #111111);
    border-radius: 24px;
    padding: 40px;
    max-width: 600px;
    width: 90%;
    border: 1px solid var(--gold);
    position: relative;
    animation: scaleIn 0.3s ease;
    max-height: 90vh;
    overflow-y: auto;
    z-index: 10000;
}

@keyframes scaleIn {
    from {
        opacity: 0;
        transform: scale(0.9);
    }
    to {
        opacity: 1;
        transform: scale(1);
    }
}

/* Кастомный скроллбар */
.modal-content::-webkit-scrollbar {
    width: 6px;
}

.modal-content::-webkit-scrollbar-track {
    background: rgba(255, 255, 255, 0.05);
    border-radius: 3px;
}

.modal-content::-webkit-scrollbar-thumb {
    background: var(--gold);
    border-radius: 3px;
}

/* Header */
.modal-header {
    text-align: center;
    margin-bottom: 30px;
}

.header-icon {
    width: 60px;
    height: 60px;
    background: rgba(183, 154, 31, 0.1);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 16px;
}

.header-icon svg {
    font-size: 28px;
    color: var(--gold);
}

.modal-header h2 {
    color: var(--gold);
    margin-bottom: 8px;
    font-size: 28px;
}

.modal-header p {
    color: var(--text-muted);
    font-size: 14px;
}

/* Close Button */
.modal-close {
    position: absolute;
    top: 20px;
    right: 24px;
    background: rgba(183, 154, 31, 0.1);
    border: 1px solid var(--gold);
    border-radius: 50%;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--gold);
    font-size: 20px;
    cursor: pointer;
    transition: all 0.3s ease;
    z-index: 10;
}

.modal-close:hover {
    transform: rotate(90deg);
    background: var(--gold);
    color: var(--dark);
}

/* Form Groups */
.form-group {
    margin-bottom: 20px;
}

.form-group label {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 8px;
    color: var(--gold-light);
    font-size: 14px;
    font-weight: 500;
}

.label-icon {
    font-size: 16px;
    color: var(--gold);
}

/* Inputs, Selects, Textarea */
.form-group input,
.form-group select,
.form-group textarea {
    width: 100%;
    padding: 14px 16px;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(183, 154, 31, 0.3);
    border-radius: 12px;
    color: #ffffff;
    font-size: 16px;
    transition: all 0.3s ease;
    font-family: inherit;
}

.form-group select {
    background: #1a1a1a;
    color: #ffffff;
    cursor: pointer;
}

.form-group select option {
    background: #1a1a1a;
    color: #ffffff;
    padding: 10px;
}

.form-group select option:hover {
    background: var(--gold);
    color: var(--dark);
}

.form-group input[type="date"] {
    color: #ffffff;
}

.form-group input[type="date"]::-webkit-calendar-picker-indicator {
    filter: invert(1);
    cursor: pointer;
}

.form-group input:focus,
.form-group select:focus,
.form-group textarea:focus {
    outline: none;
    border-color: var(--gold);
    background: rgba(255, 255, 255, 0.12);
}

.form-group input::placeholder,
.form-group textarea::placeholder {
    color: rgba(255, 255, 255, 0.4);
}

.form-group input:disabled,
.form-group select:disabled,
.form-group textarea:disabled {
    opacity: 0.6;
    cursor: not-allowed;
}

.form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
}

/* Hall Info Card */
.hall-info-card {
    background: rgba(255, 255, 255, 0.05);
    border-radius: 16px;
    padding: 16px;
    margin: 20px 0;
    display: flex;
    gap: 16px;
    border: 1px solid rgba(183, 154, 31, 0.3);
}

.hall-info-icon {
    font-size: 48px;
    display: flex;
    align-items: center;
    color: var(--gold);
}

.hall-info-details h4 {
    color: var(--gold);
    margin-bottom: 8px;
    font-size: 18px;
}

.hall-info-details p {
    color: var(--text-muted);
    font-size: 14px;
    margin-bottom: 4px;
}

.hall-warning {
    color: #ff9800 !important;
    margin-top: 8px;
    font-size: 12px !important;
    display: flex;
    align-items: center;
    gap: 6px;
}

.hall-warning svg {
    font-size: 14px;
}

/* Submit Button */
.submit-booking-btn {
    width: 100%;
    padding: 16px;
    font-size: 18px;
    margin-top: 10px;
    background: linear-gradient(135deg, var(--gold), var(--gold-light));
    border: none;
    border-radius: 40px;
    font-weight: 600;
    color: var(--dark);
    cursor: pointer;
    transition: all 0.3s ease;
}

.submit-booking-btn:hover:not(:disabled) {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(183, 154, 31, 0.4);
}

.submit-booking-btn:disabled {
    opacity: 0.7;
    cursor: not-allowed;
}

/* Success State */
.booking-success {
    text-align: center;
    padding: 30px 20px;
}

.success-icon {
    width: 80px;
    height: 80px;
    background: rgba(76, 175, 80, 0.1);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 20px;
}

.success-icon svg {
    font-size: 48px;
    color: #4caf50;
}

.booking-success h3 {
    font-size: 24px;
    margin-bottom: 12px;
    color: var(--text-light);
}

.booking-success p {
    color: var(--text-muted);
    margin-bottom: 24px;
}

/* Error State */
.error-msg {
    margin-top: 16px;
    padding: 12px;
    background: rgba(244, 67, 54, 0.1);
    border: 1px solid rgba(244, 67, 54, 0.3);
    border-radius: 12px;
    color: #f44336;
    text-align: center;
}

/* Spinner */
.spinner {
    display: inline-block;
    width: 18px;
    height: 18px;
    border: 2px solid var(--dark);
    border-top-color: transparent;
    border-radius: 50%;
    animation: spin 0.6s linear infinite;
    margin-right: 8px;
    vertical-align: middle;
}

@keyframes spin {
    to {
        transform: rotate(360deg);
    }
}

/* ============================================ */
/* МОБИЛЬНАЯ ВЕРСИЯ - УМЕНЬШЕННЫЕ РАЗМЕРЫ */
/* ============================================ */

/* Планшеты и маленькие экраны */
@media (max-width: 768px) {
    .modal-content {
        padding: 20px 16px;
        width: 92%;
        max-height: 85vh;
        border-radius: 20px;
    }
    
    .modal-header {
        margin-bottom: 16px;
    }
    
    .header-icon {
        width: 40px;
        height: 40px;
        margin-bottom: 10px;
    }
    
    .header-icon svg {
        font-size: 20px;
    }
    
    .modal-header h2 {
        font-size: 20px;
        margin-bottom: 4px;
    }
    
    .modal-header p {
        font-size: 12px;
        line-height: 1.4;
    }
    
    .modal-close {
        top: 12px;
        right: 12px;
        width: 32px;
        height: 32px;
        font-size: 16px;
    }
    
    .form-group {
        margin-bottom: 12px;
    }
    
    .form-group label {
        font-size: 12px;
        margin-bottom: 4px;
        gap: 4px;
    }
    
    .label-icon {
        font-size: 12px;
    }
    
    .form-group input,
    .form-group select,
    .form-group textarea {
        padding: 8px 10px;
        font-size: 14px;
        border-radius: 8px;
    }
    
    .form-row {
        gap: 8px;
    }
    
    .hall-info-card {
        padding: 10px;
        margin: 12px 0;
        gap: 8px;
        border-radius: 12px;
    }
    
    .hall-info-icon {
        font-size: 28px;
    }
    
    .hall-info-details h4 {
        font-size: 14px;
        margin-bottom: 4px;
    }
    
    .hall-info-details p {
        font-size: 11px;
        margin-bottom: 2px;
    }
    
    .hall-warning {
        font-size: 10px !important;
        margin-top: 4px;
    }
    
    textarea {
        min-height: 60px;
    }
    
    .submit-booking-btn {
        padding: 10px;
        font-size: 14px;
        margin-top: 8px;
        border-radius: 30px;
    }
    
    .booking-success {
        padding: 20px 15px;
    }
    
    .success-icon {
        width: 50px;
        height: 50px;
        margin-bottom: 12px;
    }
    
    .success-icon svg {
        font-size: 28px;
    }
    
    .booking-success h3 {
        font-size: 18px;
        margin-bottom: 8px;
    }
    
    .booking-success p {
        font-size: 12px;
        margin-bottom: 16px;
    }
    
    .btn-gold {
        padding: 8px 20px;
        font-size: 13px;
    }
    
    .error-msg {
        margin-top: 12px;
        padding: 8px;
        font-size: 12px;
    }
}

/* Для очень маленьких экранов (телефоны) */
@media (max-width: 480px) {
    .modal-content {
        padding: 16px 12px;
        width: 95%;
        max-height: 90vh;
        border-radius: 16px;
    }
    
    .modal-header {
        margin-bottom: 12px;
    }
    
    .header-icon {
        width: 36px;
        height: 36px;
        margin-bottom: 8px;
    }
    
    .header-icon svg {
        font-size: 18px;
    }
    
    .modal-header h2 {
        font-size: 18px;
    }
    
    .modal-header p {
        font-size: 11px;
    }
    
    .modal-close {
        top: 10px;
        right: 10px;
        width: 28px;
        height: 28px;
        font-size: 14px;
    }
    
    .form-group {
        margin-bottom: 10px;
    }
    
    .form-group label {
        font-size: 11px;
    }
    
    .form-group input,
    .form-group select,
    .form-group textarea {
        padding: 7px 8px;
        font-size: 13px;
    }
    
    .form-row {
        gap: 6px;
    }
    
    .hall-info-card {
        padding: 8px;
        margin: 10px 0;
        gap: 6px;
    }
    
    .hall-info-icon {
        font-size: 24px;
    }
    
    .hall-info-details h4 {
        font-size: 13px;
    }
    
    .hall-info-details p {
        font-size: 10px;
    }
    
    .submit-booking-btn {
        padding: 8px;
        font-size: 13px;
    }
    
    .booking-success {
        padding: 16px 12px;
    }
    
    .success-icon {
        width: 45px;
        height: 45px;
    }
    
    .success-icon svg {
        font-size: 24px;
    }
    
    .booking-success h3 {
        font-size: 16px;
    }
    
    .booking-success p {
        font-size: 11px;
    }
}

/* Для экранов с маленькой высотой */
@media (max-height: 600px) {
    .modal-content {
        max-height: 95vh;
        padding: 12px 16px;
    }
    
    .modal-header {
        margin-bottom: 8px;
    }
    
    .header-icon {
        width: 30px;
        height: 30px;
        margin-bottom: 6px;
    }
    
    .header-icon svg {
        font-size: 16px;
    }
    
    .modal-header h2 {
        font-size: 16px;
        margin-bottom: 2px;
    }
    
    .modal-header p {
        font-size: 10px;
    }
    
    .form-group {
        margin-bottom: 8px;
    }
    
    .form-group label {
        font-size: 10px;
        margin-bottom: 2px;
    }
    
    .form-group input,
    .form-group select,
    .form-group textarea {
        padding: 6px 8px;
        font-size: 12px;
    }
    
    .hall-info-card {
        padding: 6px;
        margin: 8px 0;
    }
    
    .hall-info-icon {
        font-size: 20px;
    }
    
    .hall-info-details h4 {
        font-size: 12px;
    }
    
    .hall-info-details p {
        font-size: 9px;
    }
    
    .submit-booking-btn {
        padding: 6px;
        font-size: 12px;
        margin-top: 6px;
    }
    
    textarea {
        min-height: 40px;
    }
}

/* Улучшения для касаний на мобильных устройствах */
@media (max-width: 768px) {
    .modal-close,
    .submit-booking-btn,
    .btn-gold,
    select,
    input[type="date"],
    input[type="text"],
    input[type="tel"],
    textarea {
        touch-action: manipulation;
    }
    
    /* Предотвращает zoom на iOS при клике */
    input, select, textarea {
        font-size: 16px !important;
    }
    
    /* Плавный скролл */
    .modal-content {
        -webkit-overflow-scrolling: touch;
    }
    
    /* Компактный вид для select */
    select {
        background-position: right 8px center;
        background-size: 12px;
        padding-right: 28px;
    }
    
    /* Уменьшаем отступ у последнего элемента формы */
    .form-group:last-of-type {
        margin-bottom: 6px;
    }
    
    /* Улучшаем читаемость placeholder */
    .form-group input::placeholder,
    .form-group textarea::placeholder {
        font-size: 12px;
    }
}

/* Для экранов 320px и меньше (iPhone SE и подобные) */
@media (max-width: 320px) {
    .modal-content {
        padding: 12px 10px;
    }
    
    .modal-header h2 {
        font-size: 16px;
    }
    
    .form-group input,
    .form-group select,
    .form-group textarea {
        padding: 6px 7px;
        font-size: 12px;
    }
    
    .submit-booking-btn {
        padding: 7px;
        font-size: 12px;
    }
    
    .hall-info-details h4 {
        font-size: 12px;
    }
    
    .hall-info-details p {
        font-size: 9px;
    }
}